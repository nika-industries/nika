[package]
name = "kv"
version = "0.1.0"
edition = "2021"
publish = false

[lints]
workspace = true

[dependencies]
slugger = { path = "../slugger" }
starc = { path = "../starc" }

miette.workspace = true
thiserror.workspace = true
futures.workspace = true

fred = { version = "9.1", optional = true }
smallvec = "1.13.2"

[dev-dependencies]
tokio = { workspace = true, features = ["full"] }

[features]
default = [ "redis" ]
redis = [ "dep:fred" ]
