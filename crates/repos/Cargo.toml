[package]
name = "repos"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
models = { path = "../models" }
db = { path = "../db" }
slugger = { path = "../slugger" }

miette.workspace = true

[lints]
workspace = true
