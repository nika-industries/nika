[package]
name = "hex"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
health = { path = "../health" }

tokio = { workspace = true, features = [ "time" ] }
tracing.workspace = true

[lints]
workspace = true
